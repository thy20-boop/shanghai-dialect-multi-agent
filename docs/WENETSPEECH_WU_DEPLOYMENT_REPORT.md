# WenetSpeech-Wu 部署与最终评估

## 模型选择

项目基于公开 WenetSpeech-Wu 资源部署了 CosyVoice2-Wu，并实际比较三个公开专家：

| 专家 | 本地平均字符准确率 | 关键实体召回 | 结论 |
| --- | ---: | ---: | --- |
| Wu-SFT | 44.64% | 100% | 三者最稳定，作为默认生成专家 |
| Wu-CPT | 42.27% | 88.10% | 不替换默认专家 |
| 韵律 Instruct | 1.95% | 88.10% | 会朗读控制指令，不用于正文生成 |

官方公开仓库未提供 Wu-SS 权重，因此没有把论文中的 Wu-SS 指标冒充成本地结果。

## 自研控制层：ShanghaiGuard-Wu

本项目没有重新训练 CosyVoice2-Wu 的底层权重，而是在公开 Wu-SFT 专家上增加了 `ShanghaiGuard-Wu` 生成专家控制层。这个控制层是项目自己的技术实现，代码位于 `src/ganagent/wu_generation_expert.py`，主要负责：

1. 识别回答风险类型：`hotline`、`public_service`、`general`。
2. 为不同风险类型选择参考音频专家：热线类只允许认证 `hotline` 参考，普通政务类允许多参考竞争。
3. 生成固定 seed 和语速的候选计划，保证实验可复现。
4. 调用 Whisper-Medium-Wu 和多智能体链路回听候选音频。
5. 使用关键词召回、字符准确率、方言信号、可疑片段和关键实体召回做候选仲裁。
6. 对非高风险任务启用保守句首赘词裁剪；热线、电话、号码任务自动禁用裁剪。
7. 将完整策略写入 `verification_report.json`，便于答辩时解释每条候选为什么被选中或被拒绝。

因此，开源模型负责提供吴语声学能力，项目自己的部分负责“生成治理”：决定怎么生成、怎么验收、什么时候不能发布。

## 本次修复

1. 将 ModelScope/WeText 缓存迁移到纯 ASCII 路径，解决 Windows 中文用户名导致 FST 无法加载的问题。
2. 按官方方式使用 WeText 文本前端、FP32 推理和官方上海话参考音频。
3. 原生吴语声学模型直接读取语义文本，不再先机械替换成方言字，避免超出训练文本分布。
4. 每句最多生成五个固定种子候选，由 `ShanghaiGuard-Wu` 调度、Whisper-Medium-Wu 回听并仲裁。
5. 字符准确率最低门槛为 75%，电话号码、身份证、派出所、户籍等关键实体要求 100% 保留。
6. 数字支持“一/幺”和“二/两”等价，但号码必须整串匹配，`812345` 不会误判为 `12345`。
7. “沪警”只有在精确 `119` 同句出现时才按 ASR 近音错误恢复为“火警”。
8. 句间增加 0.55 秒静音，降低长 MP3 中数字和下一句粘连的概率。
9. 增加参考音频专家库，普通政务句可在认证男声、女声参考之间自动仲裁。
10. 增加保守的句首幻觉裁剪：只有裁剪后二次回听得分提高才采用；电话任务自动禁用。
11. 增加第二吴语生成服务接口，可将 CosyVoice3 等兼容服务作为候选专家，而非直接替换稳定模型。

## 最终样例

| 样例 | 整段字符准确率 | 关键实体召回 | 风险状态 |
| --- | ---: | ---: | --- |
| 12-17 秒身份证求助回答 | 85.87% | 100% | 通过 |
| 57-64 秒求助电话回答 | 76.64% | 100% | 通过 |

最终音频：

- `outputs/help_12_17_reply_wenet_wu_final.mp3`
- `outputs/help_57_64_reply_wenet_wu_final.mp3`

生成与整段回听报告：

- `outputs/help_12_17_final_generation.json`
- `outputs/help_57_64_final_generation.json`
- `outputs/help_12_17_full_audio_evaluation.json`
- `outputs/help_57_64_full_audio_evaluation.json`

## 已知限制

公开 Wu-SFT 偶尔会在句首加入少量赘词。当前仲裁能保证核心语义和关键实体，但音色自然度、赘词率和商用稳定性仍不能只靠这两个样例证明。下一阶段应使用更高质量上海话参考音频、真实听测集以及公开测试集持续评估，而不是继续盲目增加训练轮数。

参考专家配置位于 `configs/wu_reference_experts.json`。只有在真实评测中通过相应领域的参考音频，才应加入 `hotline` 等高风险领域。
